利用信号到达基站的时间差，对信号源进行定位。
已知条件：
	c1）各个基站的gps坐标（有50米的方差）
	c2）忽略各个基站的高度（只考虑二维情形）
	c3）基站收到信号的概率随着与信号源的距离呈正态分布，方差为2公里（在2公里范围收到的概率为67%）

构建：
	s1）在二维空间内以原点为中心用正态分布的方法随机采样一个点，作为信号源s的位置，方差为2公里。
	s2）在3-10范围内按照二项分布选取一个整数，作为基站的个数N，方差为2公里。
	s2）以信号源s为中心按照正态分布，初始化N个位置作为N个基站的位置。
	s4）计算各个基站到信号源的距离R，并为R添加一个正态分布的随机数，具体大小为基站时钟精度与光速的乘积。
	s5）将各个距离R的值减去其最小值得到D，即为各个基站的距离差
	s6）对以上数据进行归一化处理。缩放到（-1，1）。
	s7）将每个基站的信息组合成一个10*4的矩阵，表示有10个网关，每个网关有10个特征:(x、y、是否有效、距离差D)
	s8）生成样本的标签:一个2维向量，表示信号源的x与y坐标。

构建一个神经网络，输入形状为（10，4）输出形状为（2），损失函数为mse，用上述采样的样本进行训练。
由于构建的样本不会重复，因此模型在训练集上的误差就是模型的误差。

训练结果：
	在基站的时钟方差在100ns的时候，方差在30m，当有7个基站收到数据时，模型的预测方差在50m,其预测的信号源位置经度已经达到chan算法的经度。
