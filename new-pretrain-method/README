模型参数进化法:
s1)构建一个类C，该类中包含一个模型，模型参数使用初始化器。
s2)利用类C生成50个对象，使用uniform初始化对象的参数，构成集合collection。
s3)再利用类C用uniform初始化器生成50个对象放入到collection。
s4)从训练集采集100个样本，分别求出在collection中的各个模型下的误差,保留其中50个误差最小的。
s5)重复s3,s5直到保留下来的50个对象不再发生变化。
s6)将collection中的模型进行克隆放置到collection中，得到另外50个子模型，对子模型的参数按照正态分布进行随机调节,正态分布的标准差为sigma。
s7)将sigma进行一次衰减，sigma=sigma*0.99。
s8)重复执行s6,s7,s4。直到sigma衰减到一个下限，或者保留下来的模型连续3次不发生改变。
s9)分别以collection中留下的50个模型利用梯度下降算法进行训练100轮，保留其中一般误差最小的。
s10）重复s9，直到剩余一个模型，该模型即为预训练后的模型。

优势:
	s1-s5在一定几率上把模型参数调整到最优解附近，但方差较大。
	s5-s8通过微调，将方差降低到一定程度。
	s9-s10进行训练筛选，保留比较容易训练的。
